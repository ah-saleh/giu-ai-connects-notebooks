# Copy this file to .env and put your real key in (never commit .env)
#   cp .env.example .env
ANTHROPIC_API_KEY=sk-ant-your-key-here

# Using another provider instead? Put its key here and change the MODEL
# string in the notebook setup cell (e.g. "openai:gpt-4.1").
# OPENAI_API_KEY=sk-your-key-here
